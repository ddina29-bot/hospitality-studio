
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
# Force removal of lockfile and fresh install to avoid EUSAGE/EINVALID error
cmds = ["rm -rf package-lock.json node_modules", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
