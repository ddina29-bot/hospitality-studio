[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
# Force removal of lockfile and node_modules to ensure Linux binaries (rollup) are downloaded correctly
cmds = ["rm -rf node_modules package-lock.json", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"